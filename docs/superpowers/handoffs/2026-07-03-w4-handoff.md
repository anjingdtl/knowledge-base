# 交接：第二阶段 W3 已完成 → W4 待启动（收口）

- **日期**：2026-07-03
- **上一手**：Claude（本会话）
- **交接给**：下一任 agent
- **分支**：master（8 commit 已落盘，**未 push**——待何大哥指示）
- **用户**：何大哥（中文交互，称"何大哥"；女秘书人设，灵动淘气但正事准确）

> 接手第一件事：通读本文档 → 读 `PROGRESS.md` → 按 §3 出 W4 plan 审批后再动工。

---

## 1. 一句话现状

第二阶段 **W3（中文 lexical 强化）已 100% 落地**（8 commit `e9d186f`→`a495b39`），全量回归 **1179 passed / 0 failed** 零退化（基线 1152 + W3 新增 27）。下一步是 **W4（收口）**——第二阶段最后一周，动工前**必须先出 W4 TDD plan 给何大哥审批**（铁律，见 §8）。

## 2. W3 已做完（不要重做）

8 个 commit：

| commit | 模块 |
|---|---|
| `e9d186f` | `chinese_tokenizer.py` `_ensure_lexical_dict`（jieba 词典模块级加载）+ `detect_query_language` |
| `c73501f` | `lexical_zh.py` `LexicalZh` 同义词扩展 + `hybrid_search._keyword_search` 挂载 |
| `72eec6d` | `hybrid_search._blend_search` 语种权重拆分（zh 0.7/en 0.5） |
| `911a042` | `project_setup._lexical_zh_defaults` + `write_wiki_first_layout` 模板 + `config.example` + 空 `data/lexical_zh_*.txt` |
| `b962935`/`e0a6b16` | S4 集成测试（原 false-positive → fix 真验证机制） |
| `a495b39` | final-review minor cleanups（删冗余 import + `.gitignore` 否定规则） |

详见 `PROGRESS.md`「Karpathy Wiki-First 对齐（第二阶段）— W3 中文 lexical 强化落地」整段。S4（机制验证）达成；S6（legacy 零变化）达成。

## 3. 接手后第一步（按序）

1. 读 `PROGRESS.md`（W3 完成状态 + W4 路线）
2. 读 phase2 spec `docs/superpowers/specs/2026-07-02-knowledge-base-karpathy-wiki-first-phase2-design.md` 的 **§6.4**（W4 任务表）+ **§3 S4**（retrieval_zh 数值验收，W3 deferred 到 W4）+ **§4.4 前置 Gap B**（文件系统 wiki lint/统计工具）
3. 读 W3 plan `docs/superpowers/plans/2026-07-03-knowledge-base-karpathy-wiki-first-phase2-w3.md` 学习 TDD 范式与"架构决策（源码核实的挂载点）"风格
4. 用 `superpowers:writing-plans` 把 W4 展开成 bite-sized TDD plan
5. **进 plan mode**，`ExitPlanMode` 提交 W4 plan 给何大哥审批
6. **审批后才动工**，用 `superpowers:subagent-driven-development` 逐 Task 驱动

> W3 的源码核实方法论可复用：派 Workflow N-agent 并行核实挂载点（精确到 file:line），再据此写零 placeholder plan。

## 4. W4 任务概要（收口）

第二阶段最后一周。含三类工作：

### 4.1 Gap B：文件系统 wiki 测量基础设施（前置，spec §6.4 前置）

`shinehe wiki lint`（`src/services/wiki_lint.py`）查旧 SQLite `wiki_pages` 表，对文件系统 `wiki/*.md` **无效**。`run_wiki_eval.py` 5 指标仅 `source_coverage`/`query_save_rate` 对文件系统有效。W4 要先实现一个扫描 `wiki/*.md` 的 lint/统计工具（cross_link / orphan / source_coverage / stale 等），否则 size_aware/wiki_parent 收益无法量化。

### 4.2 retrieval eval 扩展（spec §6.4 Task 4.1/4.2）

- size_aware 路由准确率指标（小/大查询路由判定可量化）
- **`retrieval_zh` Recall@5 ≥ 0.7 真实数值验收**（W3 deferred）——需真实 `retrieval_zh` 数据集 + `reindex_all`（W3 词典只对新写入生效，存量 block_fts 需重建索引才能享受专名分词）。这是 W3 S4 数值的真正落地点。

### 4.3 文档 + 版本（spec §6.4 Task 4.3/4.4）

- `docs/advanced-features.md` 增规模自适应/wiki parent/中文 lexical 章节
- 文档-配置一致性测试
- `src/version.py` → v1.5.0
- 全量 pytest 绿 + CI 五项绿

## 5. 关键上下文与坑（必读）

### 5.1 Gap B 是 W4 前置（spec §4.4 + §6.4）

文件系统 wiki 缺测量基础设施。W3 的 `wiki/` 真实产物在会话间被清理（W2 接手时发现），当前 `wiki/` 不存在。W4 的 wiki lint 工具要扫 `wiki/*.md`，测试需自建 fixture（仿 W2/W3 测试）。端到端冒烟需用户先 `shinehe init && shinehe migrate`（配 LLM Key 补 entity/concept 页）。

### 5.2 S4 数值验收（W3 deferred 到 W4）

W3 的 S4 用集成测试验证了**机制**（dict/synonym/language-weight 真生效），但**数值** retrieval_zh 0.6→0.7 没在 eval 上验证（OfflineIndex 不走 hybrid_search）。W4 要让真实 retrieval_zh Recall@5 ≥ 0.7：
- 跑 `python evals/run_retrieval_eval.py --dataset retrieval_zh`（当前 OfflineIndex 数字 0.6，**不反映 W3 强化**）
- 要让 eval 反映 W3，**要么**强化 OfflineIndex（W3 否决，破坏英文基线），**要么**改 eval 走真实 hybrid_search（需 mock embedding+db+reindex，重），**要么**新建一个走真实 hybrid_search 的 eval 路径。这是 W4 的设计决策点，需 plan 审批时定。
- 生产真实提升需用户配真实数据 + `reindex_all`（W3 词典只对新写入生效）。

### 5.3 浅合并坑（W1/W2/W3 三次验证）

`project_setup` 新配置段要放进 `_build_local_config`/`_build_provider_config` 各自的 rag dict，**不能放进 `_wiki_first_defaults`**（`config.update` 浅合并会整体覆盖 rag 段）。W4 若加新配置段（如 eval 相关），照此范式。

### 5.4 version → v1.5.0

`src/version.py` VERSION 改 1.5.0。改完跑 `python scripts/build_docs.py`（生成用户说明文档）确认版本号一致。

## 6. 验证命令

```bash
# W3 测试（确认前任工作完好）
pytest tests/test_language_detect.py tests/test_lexical_loader.py \
       tests/test_lexical_zh_synonym.py tests/test_language_weight.py \
       tests/test_lexical_zh_layout.py tests/test_lexical_zh_integration.py -v

# W4 期间：wiki lint 工具单测 + eval
pytest tests/test_wiki_lint.py tests/test_wiki_eval.py -v   # （W4 新建）
python evals/run_retrieval_eval.py --dataset retrieval_zh    # 当前 0.6，W4 要 ≥0.7

# 全量回归（基线 1179 passed / 1 skipped）
pytest tests/ -q
```

## 7. 真实数据与产物（untracked，勿误删）

- `wiki/` —— **当前不存在**（W2 接手时发现交接记录的 11 source 页 untracked 产物在会话间被清理）。W3 测试自建 fixture，不依赖真实 wiki/。W4 wiki lint 工具测试也自建 fixture；端到端需 `shinehe init && shinehe migrate` 重建
- `data/kb.db` —— legacy 布局（W3 未改）
- `data/lexical_zh_dict.txt` + `data/lexical_zh_synonyms.txt` —— W3 提交的空模板（`.gitignore` 已加否定规则 `!/data/lexical_zh_*.txt`）
- 本机 **LLM Key 未配**，entity/concept 页未编译

## 8. 何大哥的规矩（memory，本项目共享）

- **[[plan-approval-before-work]]**：复杂/动数据/动已交付代码/多步骤任务，**动工前先出完整 plan 审批**（进 plan mode → ExitPlanMode），点头才动手；执行中遇计划外关键决策暂停确认。纯调研/单行修复/读操作不必走 plan。
- **[[let-claude-decide-tech-tradeoffs]]**：技术权衡（稳定/准确/可用）**自主拍板**，别把选择题抛回给何大哥；执行流程要走 plan。两者互补。（W3 自主拍板了：词典模块级加载纠偏 spec 字面；S4 用集成测试方案 b 偏离 spec eval；归一化效应无需调参——均记录在 W3 plan 架构决策表）
- **[[migration-use-stamp]]**：db 迁移用 `alembic stamp` 而非 `upgrade`（db.py 建表领先 alembic）。
- 中文交互，称"何大哥"，女秘书人设（灵动淘气，技术内容准确，俏皮是调味料）。

## 9. W4 之后的路线

W4（收口：Gap B 测量基础设施 + eval 扩展 + 文档 + 全量回归 → **v1.5.0**）后，第二阶段完成。评估报告点名的 Karpathy 核心原则全部落地（第一阶段知识累积层 + 第二阶段检索执行层）。剩余工程基础设施项（向量库迁移/缓存/可观测性/脱敏/夜测）留**第三阶段**独立 spec。

## 10. GitNexus 规矩（动现有符号前）

W4 改 `wiki_lint.py` / `run_retrieval_eval.py` / `version.py` 等前，必须 `gitnexus impact` 评估 blast radius（repo=`ClaudeCodeWorkSpace`，多 repo 索引要指定）。HIGH/CRITICAL 先警告何大哥再继续。改完 `gitnexus detect_changes()` 验证影响范围。
