# 交接：第二阶段 W2 已完成 → W3 待启动

- **日期**：2026-07-03
- **上一手**：Claude（本会话）
- **交接给**：下一任 agent
- **分支**：master（5 commit 已落盘，**未 push**——待何大哥指示）
- **用户**：何大哥（中文交互，称"何大哥"；女秘书人设，灵动淘气但正事准确）

> 接手第一件事：通读本文档 → 读 `PROGRESS.md` → 按 §3 出 W3 plan 审批后再动工。

---

## 1. 一句话现状

第二阶段 **W2（wiki parent-child 检索）已 100% 落地**（5 commit `b0c706f`→`36e1887`），全量回归 **1152 passed / 0 failed** 零退化（基线 1126 + W2 新增 26）。下一步是 **W3（中文 lexical 强化）**，动工前**必须先出 W3 TDD plan 给何大哥审批**（铁律，见 §8）。

## 2. W2 已做完（不要重做）

5 个 commit：

| commit | 模块 |
|---|---|
| `b0c706f` | `src/services/wiki_parent_retrieval.py` — `WikiParentRetriever.enrich`（A2: knowledge_id 回查 source + 复用 `_build_summary`，写 `parent_content`） |
| `d979f61` | `src/services/rag_pipeline.py` `WikiParentEnrichStage` — post-rerank 挂载（rerank 后、generate 前；照抄 `WikiReadStage` 门控范式） |
| `912254d` | blend 档共存契约测试（wiki/block 两路 `parent_content` 经 blend 融合不互覆盖） |
| `c2e8ab2` | `container.py`/`project_setup.py`/`config.example.yaml` — 装配 + `shinehe init` 注入 `rag.wiki_parent_child` 段 + legacy 门控 |
| `36e1887` | docs: docstring 行号引用改函数名（防漂移） |

详见 `PROGRESS.md`「Karpathy Wiki-First 对齐（第二阶段）— W2 wiki parent-child 落地」整段。spec 验收 S3 / S6 已达成。

## 3. 接手后第一步（按序）

1. 读 `PROGRESS.md`（W2 完成状态 + W3-W4 路线）
2. 读 phase2 spec `docs/superpowers/specs/2026-07-02-knowledge-base-karpathy-wiki-first-phase2-design.md` 的 **§4.3**（W3 中文 lexical 设计）+ **§6.3**（W3 任务表）+ **§3 S4**（验收 Recall@5 ≥ 0.7）
3. 读 W2 plan `docs/superpowers/plans/2026-07-02-knowledge-base-karpathy-wiki-first-phase2-w2.md` 学习 TDD 范式与"架构决策（源码核实的挂载点）"风格
4. 用 `superpowers:writing-plans` 把 W3 展开成 bite-sized TDD plan（仿 W2 plan 结构：Context / 架构决策 / Global Constraints / File Structure / Task N.N.N step / 验证 / 风险 / Self-Review）
5. **进 plan mode**，`ExitPlanMode` 提交 W3 plan 给何大哥审批
6. **审批后才动工**，用 `superpowers:subagent-driven-development` 逐 Task 驱动（implementer + reviewer per task + final whole-branch review）

> W2 的源码核实方法论可复用：派 Workflow 4-agent 并行核实挂载点（字段名/stage 时序/DB 获取/container 注入），结论精确到 file:line，再据此写零 placeholder plan。

## 4. W3 任务概要（中文 lexical 强化）

补齐 Karpathy「中文 lexical 友好」原则。当前 keyword 通道走 FTS5 + jieba，RRF 权重 `w_keyword=0.6` 固定，中文召回弱（`retrieval_zh` Recall@5 长期 0.6）。

- **3.1 专名分词**：新增 `data/lexical_zh_dict.txt`（自定义词典，可空），`hybrid_search._keyword_search` 分词前加载，提升专名召回
- **3.2 同义词扩展**：新增 `data/lexical_zh_synonyms.txt`（每行 `词 同义词1 同义词2`），query 改写时扩展同义查询并集进 FTS5；可能新增 `src/services/lexical_zh.py`
- **3.3 权重按语种**：`rag.rrf_weight_keyword` 拆为 `rrf_weight_keyword_zh`（默认 0.7，中文提权）/ `rrf_weight_keyword_en`（默认 0.5）；按查询语种选择
- **3.4 `shinehe init` 生成空字典/同义词模板 + 配置注入**（`project_setup.py`）

spec 任务：§6.3 Task 3.1-3.4。验收：**S4**（`retrieval_zh` Recall@5 ≥ 0.7）。

## 5. 关键上下文与坑（必读）

### 5.1 挂载点（spec §4.3 + §11，需 W3 plan 阶段源码核实到 file:line）

- `src/services/hybrid_search.py` 的 `_keyword_search`（spec 标 `:115`）—— 分词/同义词注入点
- `hybrid_search.py` RRF 融合段（spec 标 `:167`，`w_keyword=0.6` 固定）—— 语种权重拆分点
- 新增 `data/lexical_zh_dict.txt` + `data/lexical_zh_synonyms.txt`（纯文本，`shinehe init` 生成空模板）
- 可能新增 `src/services/lexical_zh.py`（同义词扩展模块）
- `src/services/project_setup.py`（W2 已有 `_wiki_parent_defaults` 范式可照抄：`_lexical_zh_defaults` @staticmethod + 注入两个 build 函数 rag dict，**避开 `_wiki_first_defaults` 浅合并坑**）
- `config.example.yaml`（W2 已有 `rag.wiki_parent_child` 段范式可照抄：`rag.lexical_zh` 段）

### 5.2 retrieval_zh 基线与目标

- 当前 `retrieval_zh` Recall@5 = **0.6**（PROGRESS v1.3.1 段，非零 baseline）
- W3 目标 Recall@5 ≥ **0.7**（+10%）
- eval：`evals/run_retrieval_eval.py`（CI 同款 fake-embedding 门禁）；改完跑 `retrieval_zh` 数据集确认不退化

### 5.3 字典格式（零运行时 LLM）

纯文本，`shinehe init` 生成空模板，用户按域填充；加载失败仅 warning 不阻塞检索（与 wiki hook、W2 enricher 同策略）。

### 5.4 Config.get 支持任意深度点号路径 + 浅合并坑（W1/W2 已验证）

- `Config.get("rag.lexical_zh.xxx", default)` 缺段返回 default，无需改 `config.py`
- `project_setup._lexical_zh_defaults()` 经 `config.update()` **浅合并**——新配置段要放进 `_build_local_config`/`_build_provider_config` 各自的 rag dict，**不能放进 `_wiki_first_defaults`**（W1/W2 已两次踩证）

### 5.5 不破坏英文召回

zh/en 权重独立配置（§5 风险表）；改完跑英文数据集（`retrieval_code`/`retrieval_table`，当前 Recall@5 = 1.0）确认不退化。

## 6. 验证命令

```bash
# W2 测试（确认前任工作完好）
pytest tests/test_wiki_parent_retrieval.py tests/test_wiki_parent_enrich_stage.py \
       tests/test_wiki_parent_legacy.py -v

# W3 期间：中文 lexical 单测 + retrieval_zh eval
pytest tests/test_lexical_zh.py tests/test_hybrid_search.py -v
python evals/run_retrieval_eval.py --dataset retrieval_zh

# 全量回归（基线 1152 passed / 1 skipped）
pytest tests/ -q
```

## 7. 真实数据与产物（untracked，勿误删）

- `wiki/` —— **当前不存在**（W2 接手时发现交接 §7 记录的 11 source 页 untracked 产物在会话间被清理）。W2 测试全部自建 fixture，不依赖真实 wiki/。W3 不依赖 wiki/，但若要端到端冒烟 W2，需先 `shinehe init && shinehe migrate` 重建
- `raw/` —— 同上，可能已不在
- `data/kb.db` —— legacy 布局（W2 未改）
- 本机 **LLM Key 未配**，entity/concept 页未编译（W2 enricher 对缺失 source 静默降级，已测）

## 8. 何大哥的规矩（memory，本项目共享）

- **[[plan-approval-before-work]]**：复杂/动数据/动已交付代码/多步骤任务，**动工前先出完整 plan 审批**（进 plan mode → ExitPlanMode），点头才动手；执行中遇计划外关键决策暂停确认。纯调研/单行修复/读操作不必走 plan。
- **[[let-claude-decide-tech-tradeoffs]]**：技术权衡（稳定/准确/可用）**自主拍板**，别把选择题抛回给何大哥；执行流程要走 plan。两者互补。（W2 自主拍板了 3 处：字段名 `parent_content` 非 `parent_context`；配置项 `max_parent_chars` 非 `wiki_parent_context_max_length`；S3 候选级断言非 citation 级——均记录在 W2 plan 架构决策表）
- **[[migration-use-stamp]]**：db 迁移用 `alembic stamp` 而非 `upgrade`（db.py 建表领先 alembic）。
- 中文交互，称"何大哥"，女秘书人设（灵动淘气，技术内容准确，俏皮是调味料）。

## 9. W3 之后的路线

W3（中文 lexical，Recall@5 ≥ 0.7）→ W4（收口：含 **Gap B 文件系统 wiki 测量基础设施**（`shinehe wiki lint` 查旧 SQLite `wiki_pages` 表对 `wiki/*.md` 无效，需新建扫描 `wiki/*.md` 的 lint/统计工具）+ 文档 + 全量回归 + 版本 → v1.5.0）。每 W 末跑对应回归 + eval，全量不退化方进下一 W。

## 10. GitNexus 规矩（动现有符号前）

W3 改 `hybrid_search._keyword_search` / RRF 融合段前，必须 `gitnexus impact` 评估 blast radius（repo=`ClaudeCodeWorkSpace`，多 repo 索引要指定）。HIGH/CRITICAL 先警告何大哥再继续。改完 `gitnexus detect_changes()` 验证影响范围。
