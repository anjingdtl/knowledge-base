# requirements.txt 已弃用
# 依赖声明统一在 pyproject.toml 中管理
#
# 安装方式:
#   pip install -e ".                      # 核心依赖
#   pip install -e ".[api,parsers,wiki]"   # API + 解析器 + Wiki
#   pip install -e ".[all]"                # 全部
#   pip install -e ".[dev]"                # 开发工具
#
# 如需生成 requirements.txt:
#   pip install -e ".[all,dev]"
#   pip freeze > requirements.txt
